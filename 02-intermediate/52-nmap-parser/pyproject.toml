[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "project-52-nmap-parser"
version = "0.1.0"
description = "Nmap Result Parser & Diff"
requires-python = ">=3.11"
dependencies = ["click>=8.1"]

[project.scripts]
nmap-parser = "project_52.cli:cli"

[tool.hatch.build.targets.wheel]
packages = ["src/project_52"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
[tool.ruff.lint]
select = ["E","F","W","B","I","N","UP","S","A","C4","SIM","RUF"]
ignore = ["S101"]
